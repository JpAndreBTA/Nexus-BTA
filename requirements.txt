# NEXUS BTA backend dependency baseline.
# The current UI is static in index.html; these packages are for the next backend step.

fastapi>=0.136.3
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.8
python-multipart>=0.0.9

# ComfyUI embedded runtime baseline
comfyui-workflow-templates==0.9.75
comfyui-embedded-docs==0.5.0
comfyui-frontend-package>=1.43
aiohttp>=3.14.0
yarl>=1.18.0
pyyaml>=6.0
torchsde>=0.2.6
SQLAlchemy>=2.0
alembic>=1.13
filelock>=3.15
av>=17.0.1
requests>=2.32
simpleeval>=1.0
blake3>=1.0

--extra-index-url https://download.pytorch.org/whl/cu130
torch==2.10.0+cu130
torchvision==0.25.0+cu130
torchaudio==2.10.0+cu130
accelerate>=0.33
diffusers>=0.38.0
transformers>=4.57.6,<5
safetensors>=0.4.4
peft>=0.12
datasets>=2.21
huggingface-hub>=0.36.2
# Kaggle fallback for gated mirrors such as DINOv3 when Hugging Face access is not authorized.
kagglehub==0.3.13
tensorboard>=2.17
toml>=0.10
tomlkit>=0.13
sentencepiece>=0.2
protobuf>=5.29.6
tokenizers>=0.22.2
gguf>=0.10

numpy>=2.4.6
pillow>=12.2.0
opencv-python>=4.10.0.84
opencv-contrib-python>=4.10.0.84
imageio>=2.34
imageio-ffmpeg>=0.5
soundfile>=0.13
piexif>=1.1
einops>=0.8
tqdm>=4.66
psutil>=6.0
OpenEXR>=3.2.0
opencolorio>=2.3.0
colour-science>=0.4.4
defusedxml>=0.7.1

# LTX 2.3 IC-LoRA motion transfer preprocessors (Pose/DWPose, Canny, Depth, Cameraman).
# The KJNodes repo below provides LTXVAddGuideMulti / LTXV Img To Video Inplace KJ,
# which are required by Cameraman + end-frame / latent-upscale guide reapplication.
onnxruntime>=1.18
kornia>=0.8.2
ninja~=1.11.1.4
timm>=0.6.13
importlib_metadata>=8.0
python-dateutil>=2.9
# The ComfyUI controlnet_aux custom node ships its own custom_controlnet_aux code.
# Do not install the PyPI `controlnet-aux` package by default on Python 3.12:
# its 0.0.10 MediaPipe wrapper imports the removed `mediapipe.solutions` API.
mediapipe>=0.10.32
fvcore>=0.1.5.post20221221
yapf>=0.40
omegaconf>=2.3
ftfy>=6.2
addict>=2.4
yacs>=0.1.8
albumentations>=1.4,<2.1
scikit-image>=0.26.0
scikit-learn>=1.5
matplotlib>=3.8

# Required ComfyUI custom-node repos for LTX 2.3 IC preprocessors and Cameraman.
# Installed by scripts/install_ltx_director_deps.ps1:
# - https://github.com/Fannovel16/comfyui_controlnet_aux.git
# - https://github.com/yuvraj108c/ComfyUI-Video-Depth-Anything.git
# - https://github.com/Lightricks/ComfyUI-LTXVideo.git
# - https://github.com/Kosinkadink/ComfyUI-VideoHelperSuite.git
# - https://github.com/kijai/ComfyUI-KJNodes.git
# - https://github.com/rgthree/rgthree-comfy.git
# - https://github.com/danTheMonk/comfyui-int-and-float.git
# - https://github.com/ClownsharkBatwing/RES4LYF.git
# - https://github.com/scraed/LanPaint.git

# TRELLIS.2 / 3D MODEL template support.
# Core custom node repo: https://github.com/visualbruno/ComfyUI-Trellis2
# Model weights: https://huggingface.co/microsoft/TRELLIS.2-4B
meshlib
pymeshlab
open3d
plotly
rembg
scipy
trimesh
plyfile
zstandard
# ComfyUI-Trellis2 native Windows wheels are installed from
# custom_nodes/ComfyUI-Trellis2/wheels/Windows/<Torch build>/ when available:
# cumesh, custom_rasterizer, nvdiffrast, flex_gemm, o_voxel

# Attention/runtime acceleration. xFormers is pinned to the matching PyTorch CUDA 13.0 wheel;
# keep SDPA as fallback when the local GPU/PyTorch build cannot load these kernels.
xformers @ https://download.pytorch.org/whl/cu130/xformers-0.0.35-py39-none-win_amd64.whl
sageattention>=1.0.6
# Optional attention/runtime packages, install only when compatible with the GPU and PyTorch build:
# flash-attn

# Optional restoration/upscale/video helpers:
# realesrgan
# facexlib
# onnxruntime-gpu

# Optional Train LoRA trainer packages/repos:
# - kohya-ss/sd-scripts for SD 1.5, SDXL, Illustrious, Pony, Anima
# - ostris/ai-toolkit for Flux/Qwen style trainers
# - LTX-2 trainer for LTX 2.3 video LoRA
# - musubi-tuner for Wan video LoRA
# - bitsandbytes/prodigyopt when supported by the local CUDA/PyTorch build
