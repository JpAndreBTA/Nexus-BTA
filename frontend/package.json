{
  "name": "nexus-bta-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "rsbuild dev --host 127.0.0.1 --port 3000",
    "build": "rsbuild build",
    "preview": "rsbuild preview --host 127.0.0.1 --port 3000",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm run build"
  },
  "dependencies": {
    "@tanstack/react-query": "latest",
    "@tanstack/react-router": "latest",
    "lucide-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "zustand": "latest"
  },
  "devDependencies": {
    "@rsbuild/core": "latest",
    "@rsbuild/plugin-react": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "playwright": "^1.60.0",
    "typescript": "latest"
  }
}
